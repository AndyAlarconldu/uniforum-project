version: '3.8'
services:
  schedule-service:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/schedules_db
    volumes:
      - .:/app
    command: npm run dev

volumes:
  postgres_data: